// Creá una clase CuentaBancaria con propiedades titular y saldo,
// y métodos depositar(monto) y retirar(monto) (que valide que no se pueda retirar más de lo disponible).

class cuentaBancaria {
    constructor(titular, saldo) {
        this.titular = titular
        this.saldo = saldo
    };

    depositar(monto) {

    };

    retirar(monto) {

    };

};